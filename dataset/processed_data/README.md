# Processed data

This directory stores derived datasets (for example, `.npz` files) generated from raw data.

Expected subfolders in this project:

- `ct_256/`
- `mr_256/`

Processed files are ignored in Git to keep the repository lightweight and reproducible.
